import { DateTime } from "luxon";

export class DateTimeServiceStub {
    public timeZoneShort = 'EST';

    getTimeInMillis(startDTStr:string) {
    }

    convertToFormInputValue(startDTStr:string) {
    }
}